'use strict';

describe('uk.ac.soton.ecs.analytics.example.video module', function() {

  beforeEach(module('uk.ac.soton.ecs.analytics.example.video'));

  describe('video controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('VideoCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
